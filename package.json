{
  "name": "react-color",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "copy-to-clipboard": "^3.0.8",
    "prop-types": "^15.6.2",
    "react": "^16.6.3",
    "react-copy-to-clipboard": "^5.0.1",
    "react-dom": "^16.6.3",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "deploy-aws": "react-scripts build && aws s3 sync build/ s3://www.colorgenerator.cf --profile color-generator --acl public-read",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
